version: 2

sources:
  - name: raw_shipping_data
    database: SHIPPING
    schema: RAW
    tables:
      - name: parsed_position_reports
        description: "Raw live position pings from AIS stream"
      - name: parsed_ship_static_data
        description: "Raw static data including ship type and destination"